import { style } from "@vanilla-extract/css";

const icon = style({
    fontSize: 22,
    opacity: 0.7,
});

const posting = style({
    fontSize: 24,
});

export const keywordStyle = {
    icon,
    posting,
};
